{
  "name": "classic-wrapped-node",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "OAuth, sessions, upload, and the wrapped UI. Never parses a track title.",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-session": "^1.18.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/express-session": "^1.18.1",
    "@types/node": "^24.5.0",
    "typescript": "~6.0.2"
  }
}
